web:
  build: frontend
  command: lein figwheel dev
  ports:
   - "3449:3449"
  volumes:
   - frontend:/code
backend:
  build: .
  command: leftovers
  ports:
   - "8000:8000"
  environment:
   - PORT=8000
  links:
   - redis
redis:
  image: redis
